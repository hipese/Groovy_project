<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Boards">

	<insert id="insert">
		INSERT INTO board VALUES(default,#{title} ,#{writer}, #{contents}, default, default, #{category});
	</insert>

	<select id="selectBySeq" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE SEQ=#{seq};
	</select>
	
	<select id="selectRecent" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board order by seq desc;
	</select>
	
	<select id="selectAllCom" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE CATEGORY='전사 공지' order by seq desc;
	</select>
	
    <select id="selectAllComFree" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE CATEGORY='전사 자유' order by seq desc;
	</select>

	<select id="selectAllDept" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE CATEGORY='부서 공지' order by seq desc;
	</select>
	
	<select id="selectAllDeptFree" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE CATEGORY='부서 자유' order by seq desc;;
	</select>

	<delete id="delete">
		DELETE FROM board WHERE seq=#{seq};
	</delete>

	<update id="update">
		UPDATE board SET title=#{title}, contents=#{contents}, category=#{category} WHERE seq=#{seq};
	</update>

</mapper>