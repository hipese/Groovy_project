<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Boards">

	<insert id="insert">
		INSERT INTO board VALUES(default,#{title} ,#{writer}, #{contents}, default, default, #{category});
	</insert>

	<select id="selectAll" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board;
	</select>

	<select id="selectBySeq" resultType="com.kdt.dto.BoardDTO">
		SELECT * FROM board WHERE SEQ=#{seq};
	</select>
	
		<delete id="delete">
		DELETE FROM board WHERE seq=#{seq};
	</delete>
	
		<update id="update">
		UPDATE board SET title=#{title}, message=#{contents}, category=#{category} WHERE seq=#{seq};
	</update>

</mapper>